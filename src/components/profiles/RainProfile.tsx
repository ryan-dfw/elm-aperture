import Profile from "../Profile.tsx";

const RainProfile = () => {

    const photographer = "Rain"
    const position = "LEAD PHOTO VIDEO"
    const vFocus = 70;

    return <Profile photographer={photographer} position={position} vFocus={vFocus}/>;
};

export default RainProfile;